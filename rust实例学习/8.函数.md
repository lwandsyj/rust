1. 函数定义方式

        // 没有返回类型
        fn fnName(param:type){

        }
        // 带有返回值类型
        fn fnName(param:type)->type{

            type // 不带分号
            // 或者
            return type;
        }

2. 